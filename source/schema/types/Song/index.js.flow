// @flow
import type { Artist } from '~/schema/types/Artist'

export type Song = {
  artistId: number,
  artist?: Artist,
  title: string,
  country?: string,
  releaseDate?: Date,
  explicit?: boolean,
  price?: number
}
